<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.unisbank">
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_unisbank"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_unisbank_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_APP"
            android:value="true" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProgdiActivity"></activity>
        <activity android:name=".KompetensiActivity"></activity>
        <activity android:name=".VisiMisiActivity"></activity>
        <activity android:name=".DosenActivity"></activity>
        <activity android:name=".ProfileDosenActivity"></activity>
        <activity android:name=".AuthorActivity"></activity>
    </application>

</manifest>